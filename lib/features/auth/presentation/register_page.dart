// register_page.dart — superseded by LoginPage (combined auth page).
// The router maps /register → LoginPage(initialMode: AuthMode.signup).
// This file kept only to avoid breaking any lingering import.
export 'login_page.dart' show LoginPage, AuthMode;